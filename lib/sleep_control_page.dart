import 'package:flutter/material.dart';

class SleepControlPage extends StatelessWidget {
  const SleepControlPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Control del sueño')),
      // Agrega el contenido de la página aquí
    );
  }
}
